# Stub. Implement during the build session (see docs/SPEC.md -> Scheduling).
# Plan: cron "30 21 * * 1-5" (21:30 UTC = 5:30pm ET in summer, 4:30pm ET in winter; always after close),
# set up Python, pip install -r requirements.txt, write secret GOOGLE_SERVICE_ACCOUNT_JSON to a file,
# run `python -m bsm.pipeline`.
